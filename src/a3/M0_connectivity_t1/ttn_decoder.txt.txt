function Decoder(b, port) {

return {
peopleCount: (byteArrayToInt(b))
}
}

function byteArrayToInt (b){
  value = 0
  for (var i = 0; i < b.length; i++) {
    value = (value << 8) | b[i];
}
return value
}
